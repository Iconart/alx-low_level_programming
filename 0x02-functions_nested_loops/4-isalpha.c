#include "main.h"

/**
 * _isalpha - function to return the c lower
 *
 * @c: the character c should be checked
 *
 * Return: 0 or 1
 */

int _isalpha(int c)
{
	if (c == 'c' && c == 'C')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
