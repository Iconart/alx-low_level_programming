#include <stdio.h>

/**
 * main - Entry point
 * Description: print digig numbers of base 10
 * Return: is 0 if successful
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d\n", digit);
	}
	return (0);
}
